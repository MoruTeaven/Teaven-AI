# 2026-06-15

## 移除 protocol_type 字段
- 从 `src/types.ts` 的 `UpstreamConfig` 和 `ProviderRouteConfig` 接口中移除了 `protocol_type`
- 从 `src/config.ts` 中移除了 `protocol_type` 的验证、默认值生成和路由传递
- 从 `src/routes/admin.ts` 中移除了所有 `protocol_type` 相关的 API 序列化、表单、渲染和展示逻辑
- 管理后台中"协议类型"下拉选择器已移除，"协议"列标题改为"插件"，所有原本展示 `protocol_type` 的位置改为展示 `plugin_id`
- 更新了 `docs/configuration.md`：移除示例 JSON 中的 `protocol_type`，更新配置分层说明和字段表
- 更新了 `docs/technical-design.md`：移除示例 JSON 中的 `protocol_type`，合并字段说明到 `plugin_id`
- 更新了 `README.md`：移除"按协议类型配置"的表述
- 代码无新增 lint 错误

## Provider Plugin 改为"类型"下拉框
- 管理后台标签从"Provider Plugin"改为"类型"
- 输入控件从 `<input>` 改为 `<select>` 下拉框
- 下拉选项从 `state.overview.providers` 动态填充，显示 `name (id)` 格式
- 表格列标题从"插件"改为"类型"
- 表格中 plugin_id 展示改为显示插件名称（通过 `findProvider` 的 providers 查找），dashboard 和模型路由同理
- 添加了 `populatePluginSelect()` 和 `findProvider()` 辅助函数
- 重置表单时 select 回到第一项而不是硬编码值

## 上游 ID 默认 UUIDv7 + 对用户隐藏
- `src/config.ts` 默认上游 ID 从硬编码 `"openai-compatible-default"` 改为 `createId("up")` 生成 UUIDv7
- `src/routes/admin.ts` 导入 `createId`，`readUpstreamInput` 在未提供 ID 时自动生成 UUIDv7
- 表单中"上游 ID"字段从可见 `<input>` 改为 `type="hidden"`，对用户不可见
- 表格列标题从"ID"改为"名称"，展示上游名称而非原始 ID
- 保存时如果 hidden ID 为空则自动生成新 ID
- dashboard 渲染中也去掉了原始 ID 的展示
- `resetUpstreamForm` 清空 hidden ID 以触发新建流程

## 移除 provider（供应商标识）字段
- `provider` 是纯展示字段，与 `name`（上游名称）完全重复，不参与任何运行时逻辑
- 从 `src/types.ts` 的 `UpstreamConfig`、`ProviderRouteConfig`、`AsyncTaskRecord` 接口中移除
- 从 `src/config.ts` 的默认配置和 `toProviderRoute()` 中移除
- 从 `src/admin/store.ts` 的 `UsageRecord` 接口和两个记录函数中移除
- 从 `src/routes/admin.ts` 中移除表单字段（标签+输入框）、表格列、API 序列化/反序列化、save/reset/fill 函数
- 更新 `docs/technical-design.md` 示例 JSON、三个字段表
- 更新 `docs/configuration.md` 示例 JSON
- 代码零 linter 错误

## D1 数据库 Schema 及文档对齐
- 创建 `migrations/0001_initial_schema.sql`：完整的初始 D1 建表语句，包含 tenants、users、api_keys、gateway_configs、usage_records、async_tasks 六张表
- Schema 中已去除 `protocol_type` 和 `provider` 冗余字段
- `usage_records.plugin_id` 注释说明"插件即协议"
- `wrangler.toml` 添加 `[[d1_databases]]` 和 `[[migrations]]` 配置
- `docs/technical-design.md` 更新概念术语表中 "Protocol / Provider Plugin" → "Provider Plugin（类型）"
- `docs/technical-design.md` 将所有"按协议类型配置"改为"选择插件配置"，与代码一致
